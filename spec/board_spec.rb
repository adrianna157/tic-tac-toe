require_relative '../lib/board'

describe 'A Tic Tac Toe board' do
 let(:board) {Board.new}


  it 'needs covered with tests' do
    board = Board.new
  end

  # it 'has a place' do
  #     board = Board.new
  #     @token = stub(:token)
  #     @row = stub(:row)
  #     @column = stub(:column)
  
  #   #expect(board.place(1,2,3)).to equal(1) 
  #    expect board.place(token, row, column).to eq(nil)
  # end
  
  # it 'finds where the token is at' do
  #   expect board.token_at().to eq(nil)
  # end
  





end
